# deployment:
zmacknout plus v source control - prida vsechny soubory do "Staged Changes"

# muzes s AI navrhnout commit message
# please suggest git commit message for the staged changes, make it compatible for git commit -m ""  -so no double quotes
git commit -m "nejaka zprava"

# nahrani zmen na github PLUS je od Ondraska nastaveny automaticky deploy
git push origin main
